name = "family-clock"
main = "worker.js"
compatibility_date = "2025-09-01"

# KV namespace for kid records and tokens.
# Create with: wrangler kv namespace create CLOCK
# Then paste the returned id below.
[[kv_namespaces]]
binding = "CLOCK"
id = "8f1bcf221aea4ed7a85d7c1eff93ab3b"

# Optional: pin to a custom route on your own domain.
# [[routes]]
# pattern = "clock.example.com/*"
# zone_name = "example.com"
